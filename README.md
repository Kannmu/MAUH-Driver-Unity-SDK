# UMH Driver

Provides an interface to communicate with the UMH haptic device via serial port.

## Usage

1. Import the package into your Unity project.

2. Change the Api Compatibility Level to `.NET Standard 2.0` in `Edit->Project Settings->Player->Other Settings->Api Compatibility Level*`






